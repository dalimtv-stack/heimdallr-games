/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['fitgirl-repacks.site', 'riotpixels.com', 'via.placeholder.com']
  }
};

module.exports = nextConfig;
